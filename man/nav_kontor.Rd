% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nav_kontor.R
\docType{data}
\name{nav_kontor}
\alias{nav_kontor}
\title{Nav local offices with address and municipality}
\format{
A \code{data.frame} with 243 rows and 19 columns:
\describe{
\item{enhet_nr}{\code{character}. NORG unit number, four digits, unique key.}
\item{navn}{\code{character}. Office name as on nav.no.}
\item{type}{\code{character}. Always \code{"LOKAL"} here; see \link{nav_enheter}.}
\item{status}{\code{character}. NORG status, \code{"Aktiv"}.}
\item{orgnr}{\code{character}. Org number of the office's Brreg sub-unit; \code{NA} for 3 offices.}
\item{i_brreg}{\code{logical}. \code{orgnr} found among Nav sub-units in Enhetsregisteret.}
\item{gate, postnr, poststed}{\code{character}. Location address (NORG \code{beliggenhet}).}
\item{kommunenr, kommune}{\code{character}. Municipality number (four digits) and name.}
\item{kommunenr_kilde}{\code{character}. \code{"brreg"} or \code{"postnr"}.}
\item{antall_ansatte}{\code{integer}. Employees registered in Brreg for the sub-unit; \code{NA} if not registered.}
\item{publikumsmottak}{\code{logical}. Has at least one public reception point.}
\item{n_mottak}{\code{integer}. Number of reception points (1 to 6).}
\item{dropin}{\code{logical}. Any reception point has a weekday open without appointment.}
\item{telefon}{\code{character}. Local phone number where given.}
\item{skriftspraak}{\code{character}. \code{"NB"} or \code{"NN"}.}
\item{dato_uttrekk}{\code{Date}. Download date.}
}
}
\source{
nav.no office pages (embedded NORG record per unit), listed by
\url{https://www.nav.no/_/service/no.nav.navno/officeInfo}; Enhetsregisteret
bulk file of sub-units \url{https://data.brreg.no/enhetsregisteret/api/underenheter/lastned};
Bring post code register. Fetch and join scripts in the phd-data repo,
\verb{nav_kontor/R/}; prep in \code{data-raw/prep_nav_kontor.R}.
}
\usage{
nav_kontor
}
\description{
One row per Nav local office (NORG type \code{LOKAL}) as listed on nav.no,
with the office's NORG unit number, org number, location address,
municipality and a summary of its public reception. Snapshot of the
current state on \code{dato_uttrekk}; no history.

The unit number \code{enhet_nr} is the four-digit NORG code. For local offices
the first two digits are the county code of the pre-2020 scheme (\code{"03"}
Oslo, \code{"12"} Hordaland, \code{"18"} Nordland). This is believed to be the office
code used in Arena, to be confirmed against the register data.

Municipality number comes from Enhetsregisteret (Brreg) for the 235
offices whose org number matches a Nav sub-unit there, and from Bring's
post code register for the other 8 (\code{kommunenr_kilde}). Municipality
numbers follow the 2024 scheme.

Every local office has at least one public reception point
(\code{publikumsmottak}). \code{dropin} is \code{TRUE} if any reception point has at
least one weekday open without appointment. Details per reception point
are in \link{nav_mottak}.
}
\examples{
# Offices per county code (pre-2020 scheme)
table(substr(nav_kontor$enhet_nr, 1, 2))

# Offices with drop-in reception
table(nav_kontor$dropin)

# Look up municipality for an Arena office code
nav_kontor$kommune[match("0328", nav_kontor$enhet_nr)]

}
\seealso{
\link{nav_enheter}, \link{nav_mottak}
}
\keyword{datasets}
